programa {
  funcao inicio() {
    /*b) Receba a base e a altura de um triângulo e calcule a área.
Fórmula:
área = (base * altura) / 2*/
real base, altura, area
escreva("qual é a altura do triângulo? ")
leia(altura)
escreva("qual é a base do triângulo? ")
leia(base)
area=(base*altura)/2
escreva("a área do triangulo será de: ",area)
  }
}
